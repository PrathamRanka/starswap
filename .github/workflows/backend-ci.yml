name: Backend CI

on:
  push:
    branches: ["main", "develop"]
    paths:
      - "server/**"
  pull_request:
    branches: ["main"]
    paths:
      - "server/**"

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./server

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "./server/package-lock.json"

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

    # Placeholder for strictly enforcing linters
    # - name: Run Linting
    #   run: npm run lint

    # Placeholder for automated tests (Jest/Supertest)
    # - name: Run Tests
    #   run: npm test

    # Verify that the build process succeeds
    # - name: Check Build
    #   run: npm run build
